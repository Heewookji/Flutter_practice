import 'package:flutter/material.dart';

import 'custom_animation_app.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return CustomAnimationApp();
  }
}
